/** 기억 추출 — 고정 접두어(버전 1) */
export function memoryExtractPrompt(): string {
  return [
    "너는 개인 비서의 장기 기억을 관리해. 아래 대화/회의 텍스트에서 앞으로도 유용할 사실만 뽑아.",
    "뽑을 것: 사용자의 선호·습관(preference, routine), 사람에 대한 정보(person: 이름·관계·역할), 결정(decision), 목표(goal), 그 밖의 안정적인 사실(fact).",
    "뽑지 말 것: 일회성 할 일, 오늘 일정, 이미 화면에 있는 데이터, 추측, 사소한 잡담.",
    "각 항목은 한국어 한 문장으로, '사용자는 …' 또는 '<이름>은 …' 형태. 중복은 합쳐. 최대 10개. 없으면 빈 배열.",
  ].join("\n");
}
